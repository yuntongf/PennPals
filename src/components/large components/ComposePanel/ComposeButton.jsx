import { Link } from "react-router-dom";

const ComposeButton = () => {
   return (
      <Link to="/Compose">
         <btn>
            Compose
         </btn>
      </Link>
   );
}

export default ComposeButton;